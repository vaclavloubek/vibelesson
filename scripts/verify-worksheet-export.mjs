import fs from 'node:fs';
function read(path){return fs.readFileSync(path,'utf8')}
function expect(condition,message){if(!condition){console.error('worksheet export verification failed:',message);process.exit(1)}}
const migration=read('supabase/migrations/20260919105521_add_printable_worksheet_entitlement.sql');
const entitlements=read('app/api/entitlements/route.ts');
const workspace=read('components/LessonWorkspace.tsx');
const dialog=read('components/WorksheetExportDialog.tsx');
const worksheetPage=read('app/lessons/[id]/worksheet/page.tsx');
const toolbar=read('app/lessons/[id]/worksheet/WorksheetPrintToolbar.tsx');
const pdfRoute=read('app/api/lessons/[id]/worksheet-pdf/route.ts');
const worksheetReader=read('lib/lesson-worksheet-reader.ts');
const pdfBuilder=read('lib/worksheet-pdf.ts');
const pricing=read('components/PricingPage.tsx');
const packageJson=read('package.json');
const worksheetCss=read('app/lessons/[id]/worksheet/WorksheetPage.module.css');
const cookieConsent=read('components/CookieConsent.tsx');
expect(migration.includes("code in ('teacher_pro', 'admin')"),'Teacher Pro and admin must receive the worksheet entitlement.');
expect(/worksheet_export_enabled\s*=\s*v_worksheet_export/.test(migration),'Plan recomputation must persist the worksheet entitlement.');
expect(migration.includes('v_override.worksheet_export_enabled'),'Manual overrides must support worksheet export.');
expect(entitlements.includes('worksheet_export_enabled')&&entitlements.includes('worksheetExportEnabled'),'Entitlements API must expose worksheet export.');
expect(workspace.includes('WorksheetExportDialog')&&workspace.includes('worksheetExportEnabled'),'Lesson workspace must consume worksheet entitlement.');
expect(dialog.includes("window.open('/lessons/'")&&dialog.includes('Teacher Pro, School and Campus'),'Worksheet dialog must open the protected route and explain eligible plans.');
expect(worksheetPage.includes('readLessonWorksheet(supabase, userId, id)'),'Worksheet page must use the selected backend reader.');
expect(worksheetReader.includes('and owner_id = ${userId}')&&worksheetReader.includes(".eq('owner_id', userId)"),'Worksheet reader must scope both backend paths to the owner.');
expect(worksheetPage.includes("select('role, worksheet_export_enabled')"),'Worksheet page must enforce entitlement server-side.');
expect(worksheetPage.includes("profileResult.data.role === 'admin' || profileResult.data.worksheet_export_enabled"),'Worksheet route must fail closed.');
expect(!toolbar.includes('window.print()'),'Worksheet PDF output must not depend on browser window.print().');
expect(toolbar.includes('/worksheet-pdf?')&&toolbar.includes('Download PDF'),'Worksheet toolbar must use the dedicated PDF endpoint.');
expect(pdfRoute.includes('readLessonWorksheet(supabase, userId, id)'),'Worksheet PDF endpoint must use the selected backend reader.');
expect(pdfRoute.includes("select('role, worksheet_export_enabled')"),'Worksheet PDF endpoint must enforce entitlement server-side.');
expect(pdfRoute.includes("profileResult.data.role === 'admin' || profileResult.data.worksheet_export_enabled"),'Worksheet PDF endpoint must fail closed for non-entitled users.');
expect(pdfRoute.includes("'Content-Type': 'application/pdf'"),'Worksheet PDF endpoint must return application/pdf.');
expect(pdfBuilder.includes('dontBreakRows: true'),'Server PDF must keep each activity together as a non-breaking row.');
expect(worksheetPage.includes("blocks.map(({ block }, worksheetIndex)")&&worksheetPage.includes('{worksheetIndex + 1}'),'Worksheet preview must renumber selected activities sequentially.');
expect(pdfBuilder.includes("blocks.forEach(({ block }, worksheetIndex)")&&pdfBuilder.includes('activityNode(block, worksheetIndex'),'Server PDF must renumber selected activities sequentially.');
expect(pdfBuilder.includes('pdfMake.addVirtualFileSystem'),'Server PDF must embed its font through the bundled virtual file system.');
expect(!worksheetCss.includes('body>:not(#main-content)'),'Print CSS must not hide the worksheet root through a fragile :not() body selector.');
expect(!worksheetCss.includes('body>#main-content')&&!worksheetCss.includes('body>#main-content~*'),'Worksheet print CSS must not hide or force-display any body-level application wrapper.');
expect(cookieConsent.includes('WORKSHEET_PATH')&&cookieConsent.includes('if (!ready || worksheetRoute) return null;'),'Cookie UI must not render on the dedicated worksheet route.');
expect(worksheetCss.includes('.activities{display:block'),'Print layout must leave CSS Grid so page-break rules work reliably.');
expect(worksheetCss.includes('break-inside:avoid-page')&&worksheetCss.includes('page-break-inside:avoid'),'Each worksheet activity must stay together on one printed page whenever it fits on A4.');
const teacher=pricing.slice(pricing.indexOf("id: 'teacher'"),pricing.indexOf("id: 'teacher-pro'"));
const teacherPro=pricing.slice(pricing.indexOf("id: 'teacher-pro'"),pricing.indexOf('const schoolPlansCs'));
const team=pricing.slice(pricing.indexOf("id: 'team'"),pricing.indexOf("id: 'school'"));
const school=pricing.slice(pricing.indexOf("id: 'school'"),pricing.indexOf("id: 'campus'"));
const campus=pricing.slice(pricing.indexOf("id: 'campus'"),pricing.indexOf('const PLAN_TRANSLATIONS'));
expect(!teacher.includes('Pracovní listy z každé lekce'),'Teacher must not advertise worksheet export.');
expect(teacherPro.includes('Pracovní listy z každé lekce · tisk a PDF'),'Teacher Pro must advertise worksheet export.');
expect(!team.includes('Pracovní listy z každé lekce'),'Team must not advertise worksheet export.');
expect(school.includes('Pracovní listy z každé lekce · tisk a PDF'),'School must advertise worksheet export.');
expect(campus.includes('Pracovní listy z každé lekce · tisk a PDF'),'Campus must advertise worksheet export.');
expect(pricing.includes('styles.newFeatureBadge'),'Pricing must highlight worksheet feature.');
expect(packageJson.includes('node scripts/verify-worksheet-export.mjs'),'npm check must run worksheet regression check.');
expect(packageJson.includes('"pdfmake": "0.3.11"'),'Direct worksheet PDF generation must pin pdfmake.');
expect(packageJson.includes('verify-worksheet-pdf-runtime.mjs'),'Build/check must exercise actual PDF generation.');
console.log('worksheet export verification passed');
